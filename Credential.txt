ashishka1133@gmail.com\ashish$04 (Idp)
ashishka1122@gmail.com\ashish$04 (Sp)

GITHUB
ashishkagithub\ashish$1

GIT BASH command:

1) git clone <Remote rep URL>
2) After clone, it is mandatory to create new file and commit. After that only you will be able to create branches 
echo "# AADemo-1" >> README.md
3) git init
4) git add README.md
git commit -m "first commit"
git branch myfeaturebranch [Create new branch]
git checkout myfeaturebranch
git add README.md [Move the file from the working tree to the staging area]
cd into the new directory created by the clone operation
git checkout myfeaturebranch [to work in specific branch]
Git status
git commit -m “My first commit" [Commit file in local]
git push -u origin myfeaturebranch [Send Changes to the Remote Repository]


OTHER COMMAND

$ git config --global user.name "First Last" 
$ git config --global user.email you@email.com
git config --list [To check the settings]
git branch myfeaturebranch [Create new branch]
git checkout myfeaturebranch
git add README.md [Move the file from the working tree to the staging area]
Git status
git commit -m “My first commit" [Commit file in local]
git push -u origin myfeaturebranch [Send Changes to the Remote Repository]
